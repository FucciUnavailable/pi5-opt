services:
  dashboard:
    container_name: dashboard
    image: nginx:alpine
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    ports:
      - "127.0.0.1:8888:80"
    restart: unless-stopped
