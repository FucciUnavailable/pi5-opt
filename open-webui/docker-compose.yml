services:
  open-webui:
    container_name: open-webui
    image: ghcr.io/open-webui/open-webui:main
    volumes:
      - ./data:/app/backend/data
    ports:
      - "3002:8080"
    environment:
      - OLLAMA_BASE_URL=https://api.openai.com/v1  # Or Anthropic, etc.
    restart: unless-stopped
